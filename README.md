# Multi-Region Kubernetes Platform

This project implements a highly available, multi-region Kubernetes platform using Infrastructure as Code (Terraform), GitOps (ArgoCD), and a replicated data layer.

Key goals:
- Deploy Kubernetes clusters in multiple regions
- Use global traffic routing for low latency
- Synchronize applications using GitOps
- Enable disaster recovery and observability
